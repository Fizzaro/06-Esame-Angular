import { AnteprimaDirective } from './anteprima.directive';

describe('AnteprimaDirective', () => {
  it('should create an instance', () => {
    const directive = new AnteprimaDirective();
    expect(directive).toBeTruthy();
  });
});
