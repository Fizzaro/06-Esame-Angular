import { PulsanteDirective } from './pulsante.directive';

describe('PulsanteDirective', () => {
  it('should create an instance', () => {
    const directive = new PulsanteDirective();
    expect(directive).toBeTruthy();
  });
});
